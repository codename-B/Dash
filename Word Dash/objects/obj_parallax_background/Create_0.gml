/// obj_parallax_background

// vars you can configure in the object
parallax_x = 0.25;   // how much horizontal scroll to apply (0.25 = slower, 1 = same as foreground)
parallax_y = 0.25;   // how much vertical scroll to apply
sprite_index = newspaper_background; // set this in the editor
